<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kosa.member.MemberMapper">
    <select id="selectByMemberId" parameterType="Long" resultType="MyArtistDTO">
        SELECT member_id, member_email, member_pwd, member_nickname, member_image, member_email_alert,
        member_created_at, member_deleted_at, member_total_price, member_status_id
        FROM member
        WHERE member_id=#{memberId}
    </select>

</mapper>