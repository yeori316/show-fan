<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kosa.show.ShowMapper">
	<select id="selectById"
		resultType="com.kosa.showfan.show.dto.ShowDTO">
		SELECT * FROM show WHERE show_id=#{aaa}
	</select>
	
	<select id="selectAll" resultType="com.kosa.showfan.show.dto.ShowDTO"  parameterType="java.util.List">
		SELECT * FROM SHOW ORDER BY SHOW_START_DAY DESC ,GENRE_ID ASC
	</select>
</mapper>